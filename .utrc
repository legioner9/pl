#!/usr/bin/bash

repo= 
read -rp "Dowload from gh or gf ?" repo
case "$repo" in
        "gh")
        command ...
        ;;
        "gf")
        command ...
        ;;
    *)
        command ...
        ;;
esac